{
    "AutoLaunchProtocolsFromOrigins": [
        {
            "protocol": "rtp",
            "allowed_origins": [
                // FIXME: Should we add more of the PrisonPC server's hostnames?
                "prisonpc",
                "prisonpc.lan"
            ]
        }
    ]
}

